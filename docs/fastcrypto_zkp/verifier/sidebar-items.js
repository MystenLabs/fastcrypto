window.SIDEBAR_ITEMS = {"fn":[["process_vk_special","Takes an input [`ark_groth16::VerifyingKey`] `vk` and returns a `PreparedVerifyingKey`. This is roughly homologous to [`ark_groth16::PreparedVerifyingKey::process_vk`], but uses a blst representation of the elements."],["verify_with_processed_vk","Returns the validity of the Groth16 proof passed as argument. The format of the inputs is assumed to be in arkworks format. See [`multipairing_with_processed_vk`] for the actual pairing computation details."]],"struct":[["PreparedVerifyingKey","This is a helper function to store a pre-processed version of the verifying key. This is roughly homologous to [`ark_groth16::PreparedVerifyingKey`]. Note that contrary to Arkworks, we don’t store a “prepared” version of the gamma_g2_neg_pc, delta_g2_neg_pc fields, because we can’t use them with blst’s pairing engine."]]};